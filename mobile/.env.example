# Copy to .env.local. Everything here is public (EXPO_PUBLIC_* is inlined
# into the JS bundle), so never put the service-role key in this file.

# The live Next.js deployment. Every /api route the app calls lives here.
EXPO_PUBLIC_WEYN_API_URL=https://goweyn.com

# Same project the web app uses — mobile is an additional client, not a new backend.
EXPO_PUBLIC_SUPABASE_URL=https://itnonbrlfoafqhknncit.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_lA14C58eXlCuL0Q4j0Y7Zw_wXWghRul
