# Repo-level placeholders so the app boots before real secrets land.
# The platform-managed /run/base44/app.env (listed AFTER this file in compose)
# always overrides these with the real values.
SUPABASE_SERVICE_ROLE_KEY=placeholder
ADMIN_PASSWORD=placeholder
