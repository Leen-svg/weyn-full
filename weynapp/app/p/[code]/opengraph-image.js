import { ImageResponse } from "next/og";
import { db } from "@/lib/db";
export const size={width:1200,height:630};export const contentType="image/png";
export default async function Image({params}){const{code}=await params,s=db();const{data:p}=await s.from("polls").select("id").eq("code",code).maybeSingle();let names=[];if(p){const{data:o}=await s.from("poll_options").select("venues(name,neighborhood)").eq("poll_id",p.id).limit(3);names=(o||[]).map(x=>x.venues).filter(Boolean)}return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",padding:64,background:"linear-gradient(135deg,#f4efff,#fff7d6)",color:"#17283f",fontFamily:"Arial"}}><div style={{display:"flex",fontSize:34,fontWeight:800}}>WEYN · GROUP VOTE</div><div style={{display:"flex",flexDirection:"column",gap:18}}><div style={{fontSize:64,fontWeight:900}}>Vote: where are we going?</div>{names.map((v,i)=><div key={i} style={{display:"flex",fontSize:32}}>0{i+1} · {v.name} {v.neighborhood?`— ${v.neighborhood}`:""}</div>)}</div><div style={{display:"flex",fontSize:26}}>Open the link and vote, no download needed.</div></div>)}

